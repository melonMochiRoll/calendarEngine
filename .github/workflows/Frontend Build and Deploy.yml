name: Frontend Build and Deploy
run-name: 🚀 Deploying

on:
  push:
    branches: [ "release" ]

jobs:
  Build_And_Deploy:
    name: Build And Deploy
    runs-on: ubuntu-latest
    environment: production
    env:
      REACT_APP_NODE_ENV: production

    strategy:
      matrix:
        node-version: [24.x]

    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
    - name: ✅ Check out repository code
      uses: actions/checkout@v4
    
    # - name: 🧾 Use Node.js ${{ matrix.node-version }}
    #   uses: actions/setup-node@v4
    #   with:
    #     node-version: ${{ matrix.node-version }}
    #     cache: 'npm'
        
    # - name: ✨ Install dependencies
    #   run: npm ci
    
    # - name: 🔨 Build Project
    #   run: npm run build -- --mode production
    
    # - name: 💻 Get Github Actions IP
    #   id: ip
    #   uses: haythem/public-ip@v1.2

    # - name: 🔧 Set Dist Filename
    #   id: dist
    #   run: |
    #     filename="calendar-engine-front-$(date +%Y%m%d_%H%M%S)"
    #     echo "dist_filename=$filename" >> $GITHUB_OUTPUT

    - name: 🔧 Authenticate to Google Cloud
      uses: 'google-github-actions/auth@v3'
      with:
        project_id: ${{ secrets.GOOGLE_PROJECT_ID }}
        workload_identity_provider: ${{ secrets.GOOGLE_WORKLOAD_IDENTITY_PROVIDER }}

    - name: 🔧 Set up Cloud SDK
      uses: 'google-github-actions/setup-gcloud@v3'

    # - name: 📦 Compress Build Files
    #   run: tar cvf ${{ steps.dist.outputs.dist_filename }}.tar -C ./dist .

    # - name: ⬆️ Upload to R2
    #   uses: ryand56/r2-upload-action@latest
    #   with:
    #     r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}
    #     r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
    #     r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
    #     r2-bucket: ${{ secrets.R2_BUCKET }}
    #     source-dir: ./${{ steps.dist.outputs.dist_filename }}.tar
    #     destination-dir: artifacts

    - name: ✅ Deploy test
      env:
        AWS_ACCESS_KEY_ID: '${{ secrets.R2_ACCESS_KEY_ID }}'
        AWS_SECRET_ACCESS_KEY: '${{ secrets.R2_SECRET_ACCESS_KEY }}'
      run: |
        gcloud compute ssh ${{ secrets.GOOGLE_VM_NAME }} --zone=${{ secrets.GOOGLE_VM_REGION }} --command="aws s3 ls"

    # - name: 🚀 Deploy from R2
    #   run: |
    #     gcloud compute ssh ${{ secrets.GOOGLE_VM_NAME }} --zone=${{ secrets.GOOGLE_VM_REGION }} --command="
    #       aws s3 cp --region auto s3://${{ secrets.R2_BUCKET }}/artifacts/${{ steps.dist.outputs.dist_filename }}.tar /var/www/html/
    #       rm -rf dist/*
    #       tar xvf ${{ steps.dist.outputs.dist_filename }}.tar -C ./dist .
    #       rm -r ${{ steps.dist.outputs.dist_filename }}.tar
    #       sudo service nginx restart
    #     "

    # - name: ⬇️ Download from S3
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.EC2_HOST }} # 
    #     username: ${{ secrets.EC2_USERNAME }}
    #     key: ${{ secrets.EC2_SSH_PRIVATE_KEY }}
    #     port: ${{ secrets.EC2_SSH_PORT }}
    #     script_stop: true
    #     script: |
    #       aws s3 cp --region ${{ vars.AWS_REGION }} s3://${{ secrets.S3_BUCKET_NAME }}/${{ steps.dist.outputs.dist_filename }}.tar /home/ec2-user/
    #       rm -rf client_dist/*
    #       tar xvf ${{ steps.dist.outputs.dist_filename }}.tar -C ./client_dist .
    #       rm -r ${{ steps.dist.outputs.dist_filename }}.tar
    #       sudo service nginx restart
